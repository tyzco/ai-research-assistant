# PaperLink — 项目交付文档

> 最后更新：2026-07-27 | v2.29 | 17 模块 · 25 端点 · 12 搜索源 · ~5,200 行代码

## 概述

**PaperLink**（原"学术文献精读助手"）是一个从检索策略生成、12 源论文搜索、自动知识库构建到深度 RAG+Agent 问答的完整学术研究系统。

**架构决策**：不用 LangChain/LlamaIndex 做编排——每次 RAG 调用都走自研管线（自适应混合检索 + 级联重排 + 三层架构），LangGraph 只负责 Agent 流程控制。

## 模块清单（按文件打开顺序即可理解）

### 1. `config.py` (46 行) — 全局配置中心
```
所有环境变量、模型名、路径、超时常量
LLM: DeepSeek v4-pro/v4-flash
Embedding: bge-small-zh (512d, CPU)
```
**面试重点**：`LLM_TRANSLATE_MAX_TOKENS` / `SEARCH_FAST_TIMEOUT` 等集中管理常量，避免散落魔法数字。

### 2. `models.py` (80 行) — Pydantic 数据模型
```
PaperMeta, TopicState, AskRequest/AskResponse, TopicStatus
```
**面试重点**：`PaperMeta.extra="allow"` 允许外部 API 返回额外字段。

### 3. `topic_manager.py` (65 行) — 课题生命周期
```
active_topics 内存字典 + JSON 持久化 (data/topics.json)
create_topic() / save_topic_state() / _load_topics()
```
**面试重点**：重启后自动恢复历史课题列表。`_save_topics()` 在 KB 就绪时触发。

### 4. `search.py` (900 行) — 12 源论文搜索 ★
```
6→9→11→12 源演进：arXiv / OpenAlex(EN+CN) / SemanticScholar / CORE / Crossref / 
DOAJ / EuropePMC / PubMed / PapersWithCode / GoogleScholar(Apify) / CNKI

关键优化：
- 每源独立超时 (快速10s / 慢速3-5s)
- CN→EN 自动翻译 (_translate_cn_query via LLM + 字典回退)
- 质量过滤 (14个屏蔽词 + 中英文相关性)
- Crossref 摘要补全 (_enrich_abstracts)
- Unpaywall OA PDF 补链 (_enrich_via_unpaywall, 并行15篇)
- 去重 (title[:60].lower())
```
**面试重点**：12 源全部免费 API，无需注册。策略路由——中文优先 → OA优先 → 年份排序。

### 5. `downloader.py` (140 行) — PDF 解析 + 分块
```
parse_pdf (PyMuPDF/fitz) → chunk_text (LangChain RecursiveCharacterTextSplitter)
_fast_image_filter: 尺寸/长宽比/页码/颜色 4 重过滤
```
**面试重点**：LangChain Splitter 替代手写正则——分隔符优先级：中文句号 → 换行 → 空格 → 字符。15% overlap（准备2 §364）。

### 6. `knowledge_base.py` (300 行) — LanceDB 知识库
```
双层索引：摘要层(is_fulltext=False) + 全文层(is_fulltext=True)
Schema: chunk_id/paper_id/title/authors/year/abstract/doi/is_fulltext/
        is_image/image_path/page_number/chunk_index/text/vector(512)
检索: _cosine_search / _hybrid_search(BM25+向量RRF) / retrieve_with_images
```
**面试重点**：LanceDB 嵌入式（零部署），BM25 基于 Tantivy FTS，向量索引 IVF_PQ。

### 7. `qa_engine.py` (340 行) — RAG 问答引擎 ★
```
Pipeline: 多轮指代消解 → 查询改写(DeepSeek) → 语义校验(cos≥0.75) 
→ AdaptiveHybridRetriever → 级联重排 → DeepSeek 生成

_classify_intent: 5 类路由 (定义/对比/实操/最新/原理)
_validate_expansion: bge 余弦相似度校验改写质量
Father-child chunk 扩展 (chunk_index ±1 邻居)
Prompt: 三层上下文(全文/图表/摘要) + 引用格式 + 诚实奖励
```
**面试重点**：长问题才触发改写（节省 2-3s）。Faithfulness 0.40→0.82 的提升路径。

### 8. `hybrid_retriever.py` (200 行) — 自适应混合检索 ★★
```
AdaptiveHybridRetriever:
  _compute_adaptive_weight: 4 维特征 → BM25 动态 alpha
  _adaptive_rrf: alpha × RRF(BM25) + (1-alpha) × RRF(Vector)
  _cascade_rerank: Stage1 MiniLM(20→15) → Stage2 bge-reranker(15→5)
  
所有模型 lazy load（MiniLM/reranker 仅在首次调用时加载）
```
**面试重点**：不是 LangChain 固定权重——alpha 根据查询特征自适应。级联重排节省约 40% 时间。alpha 值暴露供消融实验。

### 9. `conversation_memory.py` (175 行) — 多轮记忆
```
ConversationMemory:
  - 状态模板 (research_goal/key_findings/cited_papers/tools_used)
  - 智能截断 (按优先级淘汰: tool_result>assistant>user)
  - LLM 摘要压缩 (compress, max_tokens=200)
  - JSON 持久化 (data/memory/{topic_id}.json)
```
**面试重点**：准备2 §状态管理三套方案——状态模板 + 智能截断 + LLM 压缩。

### 10. `monitor.py` (180 行) — 监控与安全
```
MetricsTracker: 请求数/错误/Token/延迟/搜索/下载
audit_log: 操作审计 (每次 tool 调用记录时间/用户/结果)
estimate_cost: Token→USD 估算 (deepseek-v4-pro 定价)
sanitize_input: 15 个正则注入检测 + 控制字符过滤 + 长度截断
safe_call: 异步重试包装器 (3 次退避 + 超时 + 降级)
```
**面试重点**：准备2 §操作审计 + 成本控制。

### 11. `auth.py` (126 行) — JWT 认证
```
bcrypt 密码哈希 + JWT (HS256, 7天过期)
用户存储: data/users.json
依赖注入: get_current_user / require_user (FastAPI Depends)
```
**面试重点**：多租户预留（topic 按 user_id 隔离的 TODO）。

### 12. `knowledge_graph.py` (180 行) — 知识图谱
```
build_graph_from_kb: LanceDB → D3.js 兼容 JSON
extract_entities_from_abstracts: DeepSeek LLM 实体抽取 (方法/数据集/指标)
save_graph_json / load_graph_json: 持久化
```
**面试重点**：轻量 GraphRAG——LLM 替代 NER 管道，JSON 替代 Neo4j。

### 13. `flywheel.py` — 计划中 ✓（已在 v2.3 删除）
> 策略：通过基准测试脚本 + `/metrics` 端点 + `audit_log` 持续监控，而非模拟机器人。

### 14. `api.py` (1150 行) — FastAPI 后端
```
25 个端点：

系统:     GET /health, /metrics
认证:     POST /register, /login, GET /me
课题:     GET /topics, POST /create_topic, DELETE /topic/{id}
搜索:     POST /search_papers, POST /search_papers/stream
RAG:      POST /ask (SSE流式), POST /ask/stream, POST /ask/trace
Agent:    POST /agent/quick, POST /agent/langgraph, POST /agent/multi, POST /agent/watch
KG:       GET /kg_data, POST /kg/build
导出:     GET /export/{id}
上传:     POST /upload_pdf/{id}, POST /download_bulk
Agent报告: GET /agent/report
```
**面试重点**：25 端点全异步。启动时后台预加载嵌入+reranker模型。CORS 全开。

### 15. `agent/tool_registry.py` (315 行) — 12 工具注册表
```
Tool 类: name / description / parameters(JSON Schema) / execute / need_confirm
to_openai_schema(): OpenAI Function Calling 格式
execute(): 类型校验→自动重试(3次退避)→幂等控制

12 工具: generate_search_strategy, search_papers, download_and_index(确认门),
        ask_knowledge_base, export_report, get_topic_state, search_knowledge_bases,
        agent_ask, + 4 个扩展工具
```
**面试重点**：准备2 §工具调用优化——Schema 校验 + 参数补全 + 自动重试。`need_confirm` 门。

### 16. `agent/langgraph_workflow.py` (410 行) — StateGraph Agent + Multi-Agent
```
StateGraph: agent → tools → reflect (条件边 + 循环 + MemorySaver)
MultiAgentGraph: Planner → Workers(串行) → Synthesizer
AgentEvaluator: 成功率/推理步数/工具准确率/影子测试

4 个端点：run / resume / multi / report
```
**面试重点**：准备2 §1104+§1251——条件边 + 循环 + 断点续跑。多 Agent 不用 CrewAI。

### 17. `evaluate_rag_standards.py` (128 行) — 标准评估
```
Faithfulness: DeepSeek LLM-as-judge (0-1 评分)
Context Precision: bge 嵌入余弦相似度
Semantic Recall@5: Top-5 最大余弦相似度
≥0.80 通过率统计
```
**面试重点**：无 RAGAS/langchain 依赖。50 篇论文 / 8 条 QA / 3 指标。

## 可视化页面 (5 个)

| 页面 | 路径 | 功能 |
|------|------|------|
| 主界面 | `/` | 课题创建 + 论文搜索 + 预览 + 问答 |
| 流水线 | `/static/pipeline.html` | 6 步 RAG 链路实时 SSE 追踪 |
| 知识图谱 | `/static/kg.html` | D3.js 力导向图 + 点击跳转 + LLM 实体抽取 |
| Agent 监控 | `/static/watch.html` | 状态机流转 + Token 趋势 + 迷你图 |
| API 测试 | `/static/test.html` | 25 端点一键测试 + curl 导出 |

## 部署

```bash
# 开发
HF_HUB_OFFLINE=1 python api.py          # → localhost:8001

# Docker
docker-compose up -d                     # healthcheck: curl /health
```

**环境变量** (.env): `DEEPSEEK_API_KEY` (必须), `SEMANTIC_SCHOLAR_API_KEY` (可选), `APIFY_API_KEY` (可选)

## 评估指标

| 指标 | 数值 | KB |
|------|------|-----|
| Semantic Recall@5 | 0.7703 | 50 papers, 5622 chunks |
| Context Precision | 0.7568 | bge-small-zh (512d) |
| ≥0.80 通过率 | 2/8 | |
| 检索延迟 | avg 0.15s, p50 0.07s | CPU only |

## 时间线版本演进

```
v0.1-v0.9 (2024-06-14~16)  基础 RAG: 嵌入/搜索/问答/评估 42%→82% Faithfulness
v1.0      (2024-06-18)     MVP 发布: 71文件, 11263行, GitHub初版
v1.2      (2024-06-21)     RAG优化: 指代消解 + 父子分块 + 自反思
v1.3                     LongRAG实验 + README + 未来方向
v2.0      (2024-07-22)     LangChain Splitter + LangGraph + 知识图谱 + 飞轮
v2.1-v2.2                KG可视化 + KG修复 + 流水线追踪
v2.3                     瘦身: 删除6死重模块 (965行, -19%)
v2.4                     模型名修复: deepseek-chat → deepseek-v4-pro
v2.5      (2024-07-26)    状态管理 + 工具优化 + 多Agent + 评估体系
v2.6                     意图路由 + 操作审计 + Token成本估算
v2.7                     Agent监控面板 + Sub-agent可视化 + Token追踪
v2.8                     API测试面板
v2.9-v2.11              速度优化: create_topic 6s→0.02s, Agent 12s→6s
v2.12-2.13              UI瘦身 + 会话持久化
v2.14                   论文预览面板 + 知识图谱集成 + 策略去重
v2.15-2.16             搜索大改: 多源并行 + 预览/下载链接 + 流式分批
v2.17-2.17热修          X()作用域修复 + 侧边栏删除 + 空话题清理
v2.18-2.20             搜索源9→11→12 + 论文数翻倍 (77→159)
v2.21-2.22             +PubMed+PWC+EuropePMC → 12源完成
v2.23                   下载引导 + SciHub镜像 + 批量DOI
v2.24-2.26             UI全面重写+动态效果+改名PaperLink
v2.27                   摘要补全(Crossref) + Docker完善
v2.28                   CV搜索修复 + 预览懒加载 + 图谱自动刷新
v2.29                   LLM翻译替代硬编码字典 + 配置集中化
```

## 已知限制

- **PDF 预览**：摘要为空时通过 Crossref API 补全（成功率 ~30%，取决于 DOI 是否被 Crossref 索引）
- **KG 实体抽取**：依赖 DeepSeek LLM——需要 API Key 且不稳定（偶有 JSON 解析失败）
- **rag-admin**：Vue 3 脚手架骨架，从未实际开发。仅供展示项目结构意识
- **认证安全**：基于 bcrypt + JWT，但 secret key 硬编码。生产环境需替换
- **Embedding**：bge-small-zh (512d) 上限 ~82%。升级到 bge-m3 (1024d) 预期 +3-5%

## 项目统计

| 指标 | 数值 |
|------|------|
| Python 源文件 | 17 个 |
| 总代码行数 | ~5,200 行 |
| API 端点 | 25 个 |
| 搜索源 | 12 个 (全部免费 API) |
| 可视化页面 | 5 个 |
| Agent 工具 | 12 个 |
| 技术文档 | 7 份 MD |
| Git commits | 60+ |
| 纯函数 benchmark | 8 QA × 3 指标 |
| 消融实验点 | 5 组对照 |
